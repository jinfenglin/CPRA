#include <stdio.h>
#include <string>
#include <stdlib.h>
#include <time.h>
using namespace std;
class tools
{
public:
	tools();
	int RandomInt();
	int RandomInt(int );
	~tools();
	void trim(string &str,char ch);
private:
};
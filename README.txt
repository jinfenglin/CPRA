index of change:
1.Population have a lot of redundancy.Because the length is short and once it is cutted it only has 6 alternative while the population is larger.Thus the population overflow cause the overlapping.

Revisement may need to do:
1.Change name of test_run function:Create a function named run to regulate,or write all the process in the test part run function. 